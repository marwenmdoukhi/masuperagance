<div class="card mb-4">
    {% if property.filename %}
        <img src="/images/properties/{{property.filename }}" class="card-img-top img-fluid " alt="" style="width: 100%; height:auto;">
    {% endif %}
    <div class="card-body">
        <h5 class="card-title">
            <a href="{{ path('property_show', {slug: property.slug, id: property.id }) }}">
                {{ property.title }}
            </a>
        </h5>
        <p class="card-text">
            {{ property.surface }} m2 - {{ property.city }} ({{ property.postalCode }})
            <br>
            <span class="text-primary" style="font-size: 2rem; font-weight: bold;">
                {{ property.formatedprix }}
            </span>
        </p>
    </div>
</div>
